name: openai
version: 1.0.0
description: "OpenAI-compatible image generation and editing backend (gpt-image-2). Saves generated images to the Hermes image cache."
author: NousResearch
kind: backend
requires_env:
  - OPENAI_IMAGE_API_KEY
